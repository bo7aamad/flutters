name: zeus_workstation
description: High-Scale Concurrent Quantitative Macro Suite.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_launcher_icons: ^0.13.1
  flutter_local_notifications: ^17.0.1
  intl: ^0.19.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/app_icon.png"
  min_sdk_android: 21